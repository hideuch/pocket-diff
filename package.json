{
  "name": "pocket-diff-monorepo",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "check": "pnpm typecheck && pnpm test && pnpm build",
    "dev": "turbo dev",
    "test": "turbo test",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.7.2"
  },
  "packageManager": "pnpm@11.20.0"
}
